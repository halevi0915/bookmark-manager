services:
  app:
    build: .
    container_name: bookmark-manager
    ports:
      - "8001:8001"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_FILE=/app/data/bookmarks.txt
    restart: unless-stopped
